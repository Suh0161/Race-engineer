# ─────────────────────────────────────────────────────────
# F1 25 Race Engineer Bot — Environment Configuration
# ─────────────────────────────────────────────────────────

# Discord Bot Credentials
DISCORD_BOT_TOKEN=
# Discord Client ID (from Developer Portal → OAuth2)
# Used to generate the invite link below — replace YOUR_CLIENT_ID:
#
#   https://discord.com/oauth2/authorize?client_id=YOUR_CLIENT_ID&permissions=3148800&scope=bot+applications.commands
#
# Permissions breakdown:
#   Connect (1048576) + Speak (2097152) + Send Messages (2048) + Embed Links (16384) + Use Slash Commands (2147483648)
DISCORD_CLIENT_ID=

# Player Discord IDs (right-click profile → Copy User ID with Developer Mode ON)
PLAYER1_DISCORD_ID=
PLAYER2_DISCORD_ID=

# Kimi AI — generates the engineer radio messages
# Get your key at: https://platform.moonshot.cn/console/api-keys
# kimi-k2-turbo-preview for .ai; kimi-k2 for .cn
KIMI_API_KEY=your_kimi_api_key_here
KIMI_MODEL=kimi-k2-turbo-preview
# .ai = international (platform.moonshot.ai), .cn = China region
KIMI_BASE_URL=https://api.moonshot.ai/v1

# ElevenLabs TTS (for voice output)
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
# Model: eleven_turbo_v2_5 (natural, fast) or eleven_multilingual_v2 (max quality)
# ELEVENLABS_MODEL=eleven_turbo_v2_5

# FFmpeg path (required for voice playback; add to PATH or set full path)
# Example for winget install: FFMPEG_PATH=C:/Users/YourName/AppData/Local/Microsoft/WinGet/Packages/Gyan.FFmpeg_Microsoft.Winget.Source_8wekyb3d8bbwe/ffmpeg-8.0.1-full_build/bin/ffmpeg.exe
# FFMPEG_PATH=ffmpeg

# F1 25 UDP Telemetry settings (must match in-game settings)
UDP_PORT=20777
UDP_FORMAT=2025

# Telemetry lost warning (when F1 25 is not running)
# TELEMETRY_LOST_WARN=false to disable the warning entirely
# TELEMETRY_LOST_COOLDOWN=600 = post at most once per 10 minutes (default)

# Database path (optional — defaults to f1_engineer.db in project root)
DATABASE_PATH=f1_engineer.db

# ── Optional fallback channel IDs ────────────────────────
# If set, the bot will try to auto-join these on startup.
# If NOT set, use /join in Discord to connect the bot manually.
# DISCORD_VOICE_CHANNEL_ID=
# DISCORD_TEXT_CHANNEL_ID=
